package site.iplease.irdserver.global.error

enum class ErrorStatus {
    ALREADY_DEMANDED_ASSIGN_IP,
    UNKNOWN_ASSIGN_IP,
    PERMISSION_DENIED,
    UNKNOWN_DEMAND,
    ALREADY_ACCEPTED_ASSIGN_IP,
    ALREADY_RESERVED_ASSIGN_IP
}
